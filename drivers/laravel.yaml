name: laravel
version: "1.0"
description: Laravel application with Nginx, PHP, Composer, Node

parameters:
  php_version:
    type: string
    default: "8.4"
    options: ["8.1", "8.2", "8.3", "8.4"]

services:
  app:
    image: "webdevops/php-nginx-dev:${php_version}"
    role: web
    volumes:
      - "${site_root}:/app"
    ports:
      - "80"
    environment:
      WEB_DOCUMENT_ROOT: "/app/public"
      PHP_MEMORY_LIMIT: "512M"
      DB_CONNECTION: "mysql"
      DB_HOST: "apod-${site_domain}-db"
      DB_PORT: "3306"
      DB_DATABASE: "${site_db_name}"
      DB_USERNAME: "${site_db_user}"
      DB_PASSWORD: "${site_db_pass}"
      APP_URL: "https://${site_domain}"
      APP_ENV: "production"

  # Queue worker — share the app image/code, run as a background process.
  # Scale with: apod process scale <domain> queue <n>
  queue:
    image: "webdevops/php-nginx-dev:${php_version}"
    role: worker
    replicas: 1
    command: "php /app/artisan queue:work --sleep=3 --tries=3 --timeout=90"
    volumes:
      - "${site_root}:/app"
    environment:
      DB_CONNECTION: "mysql"
      DB_HOST: "apod-${site_domain}-db"
      DB_PORT: "3306"
      DB_DATABASE: "${site_db_name}"
      DB_USERNAME: "${site_db_user}"
      DB_PASSWORD: "${site_db_pass}"
      APP_ENV: "production"

  # Scheduler — background singleton running Laravel's scheduler loop.
  scheduler:
    image: "webdevops/php-nginx-dev:${php_version}"
    role: scheduler
    command: "php /app/artisan schedule:work"
    volumes:
      - "${site_root}:/app"
    environment:
      DB_CONNECTION: "mysql"
      DB_HOST: "apod-${site_domain}-db"
      DB_PORT: "3306"
      DB_DATABASE: "${site_db_name}"
      DB_USERNAME: "${site_db_user}"
      DB_PASSWORD: "${site_db_pass}"
      APP_ENV: "production"

  db:
    image: "mysql:8.0"
    environment:
      MYSQL_DATABASE: "${site_db_name}"
      MYSQL_USER: "${site_db_user}"
      MYSQL_PASSWORD: "${site_db_pass}"
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
    volumes:
      - "${data_root}/mysql:/var/lib/mysql"

deploy:
  before_deploy:
    - "cd /app && composer install --no-dev --optimize-autoloader --no-interaction"
    - "cd /app && if [ -f package.json ]; then npm install && npm run build; fi"
  after_deploy:
    # The code is only present after the clone, so ensure .env and APP_KEY exist
    # here (the setup steps run before the first deploy, when /app is still empty).
    - "cd /app && if [ ! -f .env ]; then cp .env.example .env 2>/dev/null || touch .env; fi"
    - "cd /app && if ! grep -q '^APP_KEY=base64' .env; then php artisan key:generate --force; fi"
    - "cd /app && php artisan migrate --force"
    - "cd /app && php artisan storage:link 2>/dev/null || true"
    - "cd /app && php artisan optimize 2>/dev/null || true"

healthcheck:
  url: "http://localhost:80"
  interval: 10s
  timeout: 5s
  retries: 3

backup:
  paths:
    - "${site_root}"
  databases:
    - type: mysql
      service: db

setup:
  - name: "Wait for database"
    command: "sleep 10 && mysqladmin ping -h apod-${site_domain}-db --wait=30 2>/dev/null || sleep 5"
    service: app
    optional: true
  - name: "Generate .env if missing"
    command: "cd /app && if [ ! -f .env ]; then cp .env.example .env 2>/dev/null || touch .env; fi"
    service: app
  - name: "Generate app key if empty"
    command: "cd /app && if [ -f artisan ] && ! grep -q 'APP_KEY=base64' .env 2>/dev/null; then php artisan key:generate --force; fi"
    service: app
  - name: "Install dependencies"
    command: "cd /app && composer install --no-dev --optimize-autoloader --no-interaction 2>/dev/null || true"
    service: app
  - name: "Run migrations"
    command: "cd /app && php artisan migrate --force 2>/dev/null || true"
    service: app
  - name: "Fix permissions"
    command: "cd /app && chmod -R 777 storage bootstrap/cache 2>/dev/null || true"
    service: app
  - name: "Clear config cache"
    command: "cd /app && php artisan config:clear && php artisan storage:link 2>/dev/null || true"
    service: app
